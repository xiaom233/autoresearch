# 退化类型如何影响训练策略和 Loss 微调 —— 105 组实验的经验总结

## 核心论点

**退化类型与训练策略之间存在强耦合**——不能对所有退化组合使用同样的训练方式。但耦合主要发生在"训练哲学"的宏观层面（是否迁移学习、课程顺序），而非超参数细节（Loss 选择、微调方式）。

---

## 一、策略选择的三层决策框架

### 第一层：训练哲学（影响大，Δ 可达 4-7 dB）

| 策略 | 何时最优 | 何时最差 | 适用退化特征 |
|------|----------|----------|-------------|
| **Direct**（从零联合） | 退化分布特殊、不与常见退化重叠时（T3 混合顺序） | 退化间强冲突时（D3 comp+blur 崩溃 -4.85） | 退化有独特统计分布 |
| **Ft**（盲预训练→专攻） | 大部分场景——是**最安全的默认选择** | 极少差于 Direct（最多 -0.05） | 通用退化模式可迁移 |
| **Curric(正序)**（从内到外渐进） | 退化间强交互（D3 +4.27） | 弱交互退化（D1 -1.87） | 退化的前一步骤改变后一步骤的统计分布 |
| **Curric(逆序)**（从外到内剥离） | 退化间弱交互（D2 +1.95） | 强交互退化（D3 -4.27） | 退化步骤之间独立，外到内剥离天然合理 |

**结论**：选对第一层策略是唯一真正重要的事。Ft 是安全默认，但 D3 需要 Curric(正序)，D2 需要 Curric(逆序)。

### 第二层：Fine-tune 变体（影响小，Δ < 0.3 dB）

在 Ft 框架内（盲预训练→专攻），第二阶段的不同专攻方式几乎没有影响：

| 变体 | 专攻方式 | 效果 |
|------|----------|------|
| Ft（原始） | 直接全量退化 | 基线，3/6 最优 |
| FtLR | LR 降至 2e-4 | 最稳健变体，2/6 最优，从未显著差于 Ft |
| FtFreeze | 冻结浅层 | 0/6 最优，略差于 Ft |
| FtReplay | 混入 20% 随机退化 | 极不稳定：T3 并列最优，D3 崩溃 -4.79 |
| FtCurr(正序) | 渐进叠加（正序） | 最危险：D2 崩溃 -6.83 |
| FtCurr(逆序) | 渐进叠加（逆序） | D2 挽救 +4.47，但 D1/D3 仍然差 |

**结论**：如果已经选了 Ft，不要再折腾阶段 2 的变体。FtCurr 尤其危险——盲预训练已经给了足够好的特征，强制模型退回去学"更简单的任务"反而破坏通用特征。

### 第三层：Loss 选择（影响极小，Δ < 0.25 dB）

| Loss | 何时最优 | 注意事项 |
|------|----------|----------|
| l1 | 3/6 最优 | **最安全默认** |
| l1+edge | blur+noise 场景可能微弱有益（T1 +0.11） | 需灰度转换，RGB 直接算会崩溃 |
| l1+fft | 不适合 noise→blur（D2 -5.14） | 频域损失与脉冲噪声冲突 |
| mse | T3 微弱最优（+0.14） | 严重度不均匀时可能有用 |
| huber | D1 微弱最优（+0.04） | 与 l1 几乎无差异 |

**结论**：**l1 就够了**。除非有明确的先验（如"这个退化主要是压缩伪影，频域损失应该有帮助"），否则换 Loss 只是浪费时间。

---

## 二、课程学习顺序的核心机制：退化交互强度

### 弱交互退化（noise + blur）

```
noise_impulse → blur_motion 的交互：弱
原因：模糊会把噪声平滑掉，blur(noise(img)) ≈ blur(clean_img)
```

**最优策略**：Curric(逆序)——先剥外层 blur → 再剥内层 noise
**D2 上**：Curric(逆)=28.19 > Ft=27.92 > Curric(正)=26.24

### 强交互退化（comp + blur）

```
compression_jpeg → blur_lens 的交互：强
原因：JPEG 产生块效应，"被 JPEG 处理过的边缘"和"干净边缘"被 blur 抹过后完全不同
```

**最优策略**：Curric(正序)——先学去 JPEG（基础技能）→ 再叠加去 blur
**D3 上**：Curric(正)=25.26 > Curric(逆)=21.00

### 判断标准

**问自己一个问题：前一个退化会改变后一个退化的统计分布吗？**
- 不会（noise 在 blur 面前被抹平）→ 逆序，从外到内剥离
- 会（JPEG 改变了边缘，blur 又扩散了这些伪影）→ 正序，先打基础再叠加

---

## 三、多专家分步修复为什么失败

15 个独立专家 + 6 组级联联合微调，**从未超越单模型 Ft**。

三个原因：
1. **误差级联放大**：E1 的输出误差 ≈ E2 的输入噪声 → E2 的输入分布与训练时不同 → 误差逐级恶化
2. **独立训练步数不足**：每专家仅 ~4000 步，单退化虽简单但不足以建立足够好的特征
3. **联合微调太短**：~4000 步不足以纠正级联误差，需要更长的端到端训练

**当多专家可能有效时**：退化步骤之间完全正交（如去 rain + 去 haze），且每专家有足够的独立训练预算。

---

## 四、T2 策略饱和：当容量瓶颈无法绕过

T2（`noise_spatially_correlated + blur_motion + compression_jpeg_2000`）是所有退化中策略分化最小的：

| 策略 | PSNR | Δ vs 最优 |
|------|:--:|:--:|
| Ft | 19.00 | 0 |
| FtLR | 18.97 | -0.03 |
| Curric(正) | 18.89 | -0.11 |
| Curric(逆) | 18.85 | -0.15 |
| Direct | 18.89 | -0.11 |
| FtCurr(正) | 18.78 | -0.22 |
| FtCurr(逆) | 18.85 | -0.15 |

**7 种策略在 0.22 dB 内全部收敛**。这不是策略选择问题——**小模型对 T2 这种三维退化已经容量饱和**。

**判断标准**：如果最优策略的 Δ(best-worst) < 0.3 dB → 增大模型容量，别调策略。

---

## 五、实操建议

### 面对一个新的退化组合时

1. **先跑 Ft（盲预训练→专攻）**——40 分钟，这是最强基线

2. **判断退化交互强度**：
   - 看看第二个退化会不会显著改变第一个退化的视觉表现
   - 不会 → 试试 Curric(逆序)
   - 会 → 试试 Curric(正序)

3. **如果 Ft 和 Curric 差距 < 0.5 dB**：
   - 检查是否容量饱和（像 T2）→ 增大模型
   - 如果不是 → 直接用 Ft，不要花时间调策略

4. **不要做的事**：
   - 不要尝试 FtCurr（盲预训练后渐进叠加）——几乎总是更差
   - 不要在 Loss 函数上花太多时间——l1 就够了
   - 不要尝试多专家级联——单模型更好
   - 不要用 FtReplay 除非退化分布很特殊

### 唯一值得调的两个决策

| 决策 | 选项 | 何时选 |
|------|------|--------|
| 是否用盲预训练 | Ft vs Direct | Ft 是默认；退化分布特殊（如 T3）时试试 Direct |
| 课程顺序 | 正序 vs 逆序 vs 不课程 | 弱交互→逆序，强交互→正序，不确定→不做课程 |

**其他一切（Loss、微调变体、多专家）都是噪声级别的差异。**